import { Nav } from "@/components/nav";
import { LinkButton, Panel } from "@/components/ui";
import { plans } from "@/lib/plans";
export default function PricingPage(){return <div className="min-h-[100dvh] bg-zinc-50 text-zinc-950 dark:bg-zinc-950 dark:text-zinc-50"><Nav/><main className="mx-auto max-w-7xl px-4 py-16 sm:px-6"><h1 className="text-5xl font-semibold tracking-tighter">Pricing</h1><p className="mt-4 max-w-[65ch] text-zinc-500">Free tier for one test campaign. Paid plans are positioned for restaurants, real estate, events, gyms, clinics, and agencies that need dynamic destinations, print-ready exports, scan analytics, and conversion proof.</p><div className="mt-10 grid gap-4 md:grid-cols-4">{plans.map((plan)=><Panel key={plan.id}><h2 className="text-xl font-semibold">{plan.name}</h2><p className="mt-4 text-4xl font-semibold">${plan.price}<span className="text-sm text-zinc-500">/mo</span></p><p className="mt-3 min-h-12 text-sm text-zinc-500">{plan.headline}</p><ul className="mt-6 grid gap-2 text-sm">{plan.features.map((f)=><li key={f}>• {f}</li>)}</ul><LinkButton href={`/api/stripe/checkout?plan=${plan.id}`} className="mt-6 w-full">{plan.price ? "Subscribe" : "Start free"}</LinkButton></Panel>)}</div></main></div>}
